import React from 'react'
import ReactDOM from 'react-dom/client' // Esto cuenta con todos los elementos para trabajar con el DOM
import App from './App'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)
